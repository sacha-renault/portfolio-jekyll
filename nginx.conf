server {
    listen 80;
    server_name localhost;

    location /me {
        alias /usr/share/nginx/html;
        index index.html;

        # Handle pretty URLs
        try_files $uri $uri/ $uri.html =404;
    }

    location / {
        return 301 /me;
    }
}